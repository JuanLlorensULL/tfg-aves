# Plan — L1: modelo individual de 91916A (sustituye personalizado) + retirada del viento

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustituir en L1 (O4 base, `build_o4`) el modo "personalizado" (global + `bird_id`) por un modo "individual" entrenado solo sobre 91916A (patrón L3), comparado contra `poblacional@91916A`; y retirar por completo la línea del viento (L1-v1). Regenerar las figuras `o4_fig02–09`.

**Architecture:** El cambio se confina a `src/tfg_aves/ml/build.py` (orquestador L1), `features.py` (quitar viento) y `notebooks/04_eda_o4.py` (figuras). Se replica el patrón ya validado en `build_l3.py`: una sola matriz poblacional, el split del individual sale de filtrar a la ave, y el corte `poblacional@<ave>` evalúa el modelo poblacional sobre las mismas filas. L2 (`build_l2.py`) y L3 NO se tocan: conservan `include_bird_id`/`_CategoricalEncoder` y su propio `personalizado`.

**Tech Stack:** Python 3.12, pandas, scikit-learn, xgboost, joblib, pytest, jupytext. Gestor `uv`.

**Spec:** `docs/superpowers/specs/2026-05-25-o4l1-individual-91916a-design.md`.

---

## File Structure

- `src/tfg_aves/ml/build.py` — **reescritura de `build_o4`**: modos `{individual, poblacional}`, corte `poblacional@<ave>`, baselines@ave; sin viento.
- `src/tfg_aves/ml/features.py` — eliminar `merge_wind_features`, `_FEATURES_WIND` y el parámetro `wind_df`.
- `src/tfg_aves/ml/_paths.py` — eliminar `O4_L1V1_DIR`.
- `src/tfg_aves/meteo/` — **eliminar** el paquete completo.
- `tests/test_ml_build.py` — reescribir aserciones (modos individual/poblacional, corte @ave).
- `tests/test_ml_features.py` — quitar tests de `merge_wind_features`.
- `tests/test_meteo_*.py` — **eliminar**.
- `notebooks/04_eda_o4.py` — reescribir celdas C1–C8 (figs 02–09); renombrar slugs fig06/fig08.
- `notebooks/04l1_eda_o4l1.{py,ipynb}` — **eliminar**.
- `pyproject.toml` — quitar `netcdf4`, `xarray`.
- `reports/INDEX.md`, `reports/memoria/06_o4_ml.md`, `reports/ai-log/` — actualizar.
- `data/processed/o4/l1_v1/`, `data/raw/wind/`, `data/processed/wind_per_fix.parquet` — **eliminar**.
- `docs/superpowers/{specs,plans}/2026-05-23-o4l1-*` — **eliminar**.

---

## Task 1: `features.py` — eliminar el viento

**Files:**
- Modify: `src/tfg_aves/ml/features.py`
- Test: `tests/test_ml_features.py`

- [ ] **Step 1: Localizar y borrar los tests de viento (deben dejar de existir)**

Run: `grep -n "merge_wind\|wind" tests/test_ml_features.py`
Eliminar de `tests/test_ml_features.py` las funciones `test_merge_wind_features_preserves_rows` y `test_merge_wind_features_propagates_nan_when_no_match` (y cualquier import de `merge_wind_features`).

- [ ] **Step 2: Ejecutar la suite de features para ver el estado base**

Run: `uv run pytest tests/test_ml_features.py -q`
Expected: PASS (tras quitar los tests de viento; el resto sigue verde).

- [ ] **Step 3: Eliminar `_FEATURES_WIND`, `merge_wind_features` y el parámetro `wind_df`**

En `src/tfg_aves/ml/features.py`:
- Borrar el bloque de la constante `_FEATURES_WIND` (líneas ~24-26).
- Borrar la función `merge_wind_features` completa (líneas ~232-268).
- Cambiar la firma de `build_feature_matrix` a:

```python
def build_feature_matrix(
    kin: pd.DataFrame,
    cells: pd.DataFrame,
    include_bird_id: bool,
) -> pd.DataFrame:
```

- Dentro de `build_feature_matrix`, borrar las dos ramas de viento y simplificar `base`:

```python
    df = assign_cells_to_features(kin, cells)
    df = add_cyclic_doy(df)

    df = df[df["is_hmm_obs_valid"] & df["cell_id_t_next"].notna()].copy()

    base = list(FEATURES_KINEMATIC)
    feature_cols = ["bird_id", *base] if include_bird_id else list(base)
```

(El resto de la función —`keep_cols`, `out`, `attrs`— no cambia.)
Actualizar el docstring de `build_feature_matrix` para quitar la mención al paso 3 (viento).

- [ ] **Step 4: Ejecutar tests de features**

Run: `uv run pytest tests/test_ml_features.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/tfg_aves/ml/features.py tests/test_ml_features.py
git commit -m "O4/L1: elimina las features de viento de build_feature_matrix"
```

---

## Task 2: `build.py` — reescribir `build_o4` al modo individual

**Files:**
- Modify: `src/tfg_aves/ml/build.py`
- Test: `tests/test_ml_build.py`

- [ ] **Step 1: Reescribir los tests de `build_o4` (rojo)**

Reemplazar el contenido de `tests/test_ml_build.py` desde la función `test_build_o4_end_to_end` hasta el final por:

```python
def test_build_o4_end_to_end(tmp_path: Path) -> None:
    feat_path, cells_path = _write_synthetic_inputs(tmp_path)
    out_dir = tmp_path / "o4"
    result = build_o4(
        features_path=feat_path, cells_path=cells_path,
        output_dir=out_dir, seed=0, individual_bird_id="A",
    )
    assert result.n_birds == 5
    assert result.individual_bird_id == "A"
    assert result.n_rows_train > 0
    assert result.n_rows_test > 0
    assert set(result.model_paths.keys()) == {
        "individual_rf", "individual_xgb",
        "poblacional_rf", "poblacional_xgb", "poblacional_lgbm",
    }
    for p in result.model_paths.values():
        assert p.exists()

    preds = pd.read_parquet(result.predictions_path)
    assert {
        "bird_id", "date_utc", "true_cell", "pred_cell_top1", "modelo", "modo"
    } <= set(preds.columns)
    # El modo individual sólo contiene filas de la ave filtrada.
    ind = preds[preds["modo"] == "individual"]
    assert len(ind) > 0
    assert set(ind["bird_id"].unique()) == {"A"}

    metrics = pd.read_parquet(result.metrics_path)
    assert {
        "modelo", "modo", "top1", "top3", "log_loss", "dist_median_km", "split"
    } <= set(metrics.columns)
    modos = set(metrics["modo"].unique())
    assert "individual" in modos
    assert "poblacional" in modos
    assert "poblacional@A" in modos
    # Baselines recalculadas sobre la ave.
    assert "persistencia@A" in set(metrics["modelo"].unique())


def test_build_o4_idempotent(tmp_path: Path) -> None:
    feat_path, cells_path = _write_synthetic_inputs(tmp_path)
    out_dir = tmp_path / "o4"
    r1 = build_o4(features_path=feat_path, cells_path=cells_path,
                  output_dir=out_dir, seed=0, individual_bird_id="A")
    m1 = pd.read_parquet(r1.metrics_path)
    r2 = build_o4(features_path=feat_path, cells_path=cells_path,
                  output_dir=out_dir, seed=0, individual_bird_id="A")
    m2 = pd.read_parquet(r2.metrics_path)
    pd.testing.assert_frame_equal(
        m1.sort_values(["modelo", "modo", "split"]).reset_index(drop=True),
        m2.sort_values(["modelo", "modo", "split"]).reset_index(drop=True),
    )


def test_build_o4_columnas_causales(tmp_path: Path) -> None:
    feat_path, cells_path = _write_synthetic_inputs(tmp_path)
    out_dir = tmp_path / "o4"
    res = build_o4(
        features_path=feat_path, cells_path=cells_path,
        output_dir=out_dir, seed=0, individual_bird_id="A",
    )
    import joblib
    for key in ("individual_rf", "poblacional_rf"):
        blob = joblib.load(out_dir / f"model_{key}.pkl")
        cols = blob["feature_cols"]
        # Ningún modo lleva bird_id (el individual es una sola ave).
        assert "bird_id" not in cols
        for c in ["step_in_km", "sin_bearing_in", "cos_bearing_in", "cos_turning_in",
                  "state_b_causal", "posterior_b_migracion_causal"]:
            assert c in cols
        for prohibida in ["step_length_km", "cos_turning_angle", "state_b",
                          "posterior_b_migracion"]:
            assert prohibida not in cols

    preds = pd.read_parquet(res.predictions_path)
    assert "state_b_causal" in preds.columns
    assert "state_b" not in preds.columns
    assert preds[preds["modo"] == "individual"]["state_b_causal"].notna().any()
```

- [ ] **Step 2: Ejecutar para verificar fallo**

Run: `uv run pytest tests/test_ml_build.py -q`
Expected: FAIL (`build_o4` aún no acepta `individual_bird_id`; `model_paths` tiene claves `personalizado_*`).

- [ ] **Step 3: Reescribir `build.py`**

En `src/tfg_aves/ml/build.py`:

(a) Imports — quitar la línea `from ..meteo._paths import WIND_PER_FIX_PARQUET` y `O4_L1V1_DIR` del import de `._paths`; añadir `INDIVIDUAL_BIRD_ID`:

```python
from ._paths import (
    CELLS_PARQUET,
    FEATURES_O3_PARQUET,
    O4_OUT_DIR,
)
from .evaluate import (
    compare_models,
    compute_markov_baseline,
    compute_persistence_baseline,
    dist_median_km,
    evaluate_global,
    predict_with_meta,
    top_k_accuracy,
)
from .features import (
    FEATURES_HMM,
    FEATURES_KINEMATIC,
    build_feature_matrix,
    compute_causal_kinematics,
    split_temporal_per_bird,
)
from .hmm_causal import decode_causal_states, fit_causal_hmm
from .quantile import INDIVIDUAL_BIRD_ID
from .train import (
    train_lightgbm,
    train_random_forest,
    train_xgboost,
)
```

(b) Añadir el campo `individual_bird_id` al dataclass `BuildO4Result` (tras `n_rows_test`):

```python
    individual_bird_id: str = ""
```

(c) Cambiar las constantes de modo/familia:

```python
_FAMILIES = ("rf", "xgb", "lgbm")
_FAMILIES_INDIVIDUAL = ("rf", "xgb")
_MODES = ("individual", "poblacional")
```

(d) Reemplazar la función `build_o4` entera (de `def build_o4(` hasta el `return BuildO4Result(...)` final) por:

```python
def build_o4(
    features_path: Path = FEATURES_O3_PARQUET,
    cells_path: Path = CELLS_PARQUET,
    output_dir: Path = O4_OUT_DIR,
    seed: int = 0,
    *,
    individual_bird_id: str | None = None,
) -> BuildO4Result:
    """Pipeline de O4 — línea base discretizada (L1).

    Dos modos:
      - ``poblacional``: un modelo sobre las 82 aves (sin bird_id). Canónico.
        Familias RF/XGB/LGBM (LGBM se descarta en el análisis por divergencia).
      - ``individual``: un modelo entrenado y evaluado SOLO sobre
        ``individual_bird_id`` (por defecto 91916A). Familias RF/XGB.

    Se añade el corte ``poblacional@<ave>`` (el modelo poblacional restringido a
    las filas de la ave) para comparar manzanas con manzanas con el individual,
    y las baselines (persistencia, Markov) se recalculan también sobre esas filas.
    El patrón replica build_l3.py (modo individual + corte @ave).
    """
    if individual_bird_id is None:
        individual_bird_id = INDIVIDUAL_BIRD_ID
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)

    features_o3 = pd.read_parquet(features_path)
    cells = pd.read_parquet(cells_path)

    kin = compute_causal_kinematics(features_o3)

    # Una sola matriz poblacional (sin bird_id); el individual sale de filtrarla.
    matrix = build_feature_matrix(kin, cells, include_bird_id=False)
    train_pob, val_pob, test_pob = split_temporal_per_bird(matrix)

    # HMM causal: fit sobre el train temporal, decode filtrado de todo.
    cutoff_by_bird = (
        train_pob.assign(_d=pd.to_datetime(train_pob["date_utc"]))
        .groupby("bird_id")["_d"].max().to_dict()
    )
    hmm_model, hmm_labels = fit_causal_hmm(kin, cutoff_by_bird, n_restarts=10, seed=seed)
    states = decode_causal_states(hmm_model, hmm_labels, kin)

    def _attach_states(df: pd.DataFrame) -> pd.DataFrame:
        merged = df.merge(states, on=["bird_id", "date_utc"], how="left", validate="m:1")
        if merged[FEATURES_HMM].isna().any().any():
            raise ValueError("Filas candidatas sin estado HMM causal tras el merge.")
        return merged

    train_s, val_s, test_s = (_attach_states(d) for d in (train_pob, val_pob, test_pob))

    def _filter_bird(df: pd.DataFrame) -> pd.DataFrame:
        return df[df["bird_id"] == individual_bird_id].reset_index(drop=True)

    splits_by_mode = {
        "poblacional": (train_s, val_s, test_s),
        "individual": (_filter_bird(train_s), _filter_bird(val_s), _filter_bird(test_s)),
    }
    families_by_mode = {"poblacional": _FAMILIES, "individual": _FAMILIES_INDIVIDUAL}

    # Ningún modo usa bird_id: el poblacional nunca lo tuvo y el individual es una
    # sola ave (constante). feature_cols es el set causal de 10 features.
    feature_cols = [*FEATURES_KINEMATIC, *FEATURES_HMM]
    categorical_cols: list[str] = []

    metrics_per_model: dict[str, dict] = {}
    predictions_all: list[pd.DataFrame] = []
    model_paths: dict[str, Path] = {}
    pob_bundle: dict[str, tuple] = {}  # family -> (model, le_train) para el corte @ave

    for mode in _MODES:
        train, val, test = splits_by_mode[mode]
        le_train = LabelEncoder().fit(train["cell_id_t_next"].astype(str))
        known_cells = set(le_train.classes_)

        X_train, X_val, X_test = train[feature_cols], val[feature_cols], test[feature_cols]
        y_train = le_train.transform(train["cell_id_t_next"].astype(str))
        y_val = _safe_encode_cells(val["cell_id_t_next"], le_train, known_cells)

        for family in families_by_mode[mode]:
            model = _train_one(
                family, X_train, y_train, X_val, y_val,
                categorical_cols=categorical_cols, seed=seed,
            )
            metrics_test = evaluate_global(model, X_test, test, cells=cells, label_encoder_y=le_train)
            metrics_test["split"] = "test"
            metrics_train = evaluate_global(model, X_train, train, cells=cells, label_encoder_y=le_train)
            metrics_train["split"] = "train"

            key = f"{mode}_{family}"
            metrics_per_model[f"{key}::test"] = metrics_test
            metrics_per_model[f"{key}::train"] = metrics_train

            model_path = output_dir / f"model_{mode}_{family}.pkl"
            joblib.dump({
                "model": model, "label_encoder_y": le_train,
                "feature_cols": feature_cols, "categorical_cols": categorical_cols,
                "mode": mode, "family": family,
                "individual_bird_id": individual_bird_id if mode == "individual" else None,
            }, model_path)
            model_paths[key] = model_path

            preds = predict_with_meta(model, X_test, test, cells=cells, label_encoder_y=le_train)
            for k in list(preds.attrs):
                preds.attrs.pop(k, None)
            preds["modelo"] = family
            preds["modo"] = mode
            predictions_all.append(preds)

            if mode == "poblacional" and family in _FAMILIES_INDIVIDUAL:
                pob_bundle[family] = (model, le_train)

    # --- Corte poblacional@<ave>: el modelo poblacional sobre las filas de la ave ---
    train_pi, test_pi = _filter_bird(train_s), _filter_bird(test_s)
    for family, (model, le_train) in pob_bundle.items():
        for split_name, sub in (("train", train_pi), ("test", test_pi)):
            if len(sub) == 0:
                continue
            m = evaluate_global(model, sub[feature_cols], sub, cells=cells, label_encoder_y=le_train)
            m["split"] = split_name
            metrics_per_model[f"poblacional@{individual_bird_id}_{family}::{split_name}"] = m

    # --- Baselines globales sobre el split temporal de O4 ---
    persistence = compute_persistence_baseline(test_s, cells=cells)
    markov = compute_markov_baseline(train_s, test_s, cells=cells)
    persistence["modelo"] = "persistencia"; persistence["modo"] = "—"
    markov["modelo"] = "markov"; markov["modo"] = "—"
    predictions_all.extend([persistence, markov])

    def _baseline_metrics(frame: pd.DataFrame) -> dict:
        return {
            "top1": top_k_accuracy(frame, k=1),
            "top3": top_k_accuracy(frame, k=3),
            "dist_median_km": dist_median_km(frame),
            "log_loss": float("nan"),
            "split": "test",
        }

    baselines_metrics: dict[str, dict] = {
        "persistencia": _baseline_metrics(persistence),
        "markov": _baseline_metrics(markov),
    }
    pers_i = persistence[persistence["bird_id"] == individual_bird_id].reset_index(drop=True)
    markov_i = markov[markov["bird_id"] == individual_bird_id].reset_index(drop=True)
    if len(pers_i) > 0:
        baselines_metrics[f"persistencia@{individual_bird_id}"] = _baseline_metrics(pers_i)
    if len(markov_i) > 0:
        baselines_metrics[f"markov@{individual_bird_id}"] = _baseline_metrics(markov_i)

    # --- Tabla comparativa ---
    metrics_test_dict = {
        k.replace("::test", ""): v for k, v in metrics_per_model.items() if k.endswith("::test")
    }
    metrics_train_dict = {
        k.replace("::train", ""): v for k, v in metrics_per_model.items() if k.endswith("::train")
    }
    table_test = compare_models(metrics_test_dict, baselines=baselines_metrics)
    table_train = compare_models(metrics_train_dict, baselines={})
    metrics_table = pd.concat([table_test, table_train], ignore_index=True)

    # --- Guardar artefactos ---
    predictions_df = pd.concat(predictions_all, ignore_index=True)
    predictions_df["pred_cell_topk"] = predictions_df["pred_cell_topk"].apply(
        lambda x: list(x) if x is not None else []
    )
    predictions_path = output_dir / "predictions_test.parquet"
    predictions_df.to_parquet(predictions_path)
    metrics_path = output_dir / "metrics.parquet"
    metrics_table.to_parquet(metrics_path)

    return BuildO4Result(
        n_birds=int(features_o3["bird_id"].nunique()),
        n_rows_train=len(train_s),
        n_rows_val=len(val_s),
        n_rows_test=len(test_s),
        individual_bird_id=individual_bird_id,
        model_paths=model_paths,
        predictions_path=predictions_path,
        metrics_path=metrics_path,
    )
```

Nota: `compare_models` parte cada clave por el primer `_` (`"poblacional@A_rf".split("_", 1)` → `["poblacional@A", "rf"]`), por lo que `modo` puede contener `@<ave>` sin problema (los bird_id no llevan `_`).

- [ ] **Step 4: Ejecutar tests de build**

Run: `uv run pytest tests/test_ml_build.py -q`
Expected: PASS (3 tests).

- [ ] **Step 5: Verificar que L2/L3 siguen importando bien (no se rompió la firma compartida)**

Run: `uv run pytest tests/test_ml_l2_build.py tests/test_ml_l3_build.py -q` (ajustar nombres si difieren; `ls tests | grep -i l[23]`)
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/tfg_aves/ml/build.py tests/test_ml_build.py
git commit -m "O4/L1: build_o4 con modo individual 91916A y corte poblacional@ave"
```

---

## Task 3: `_paths.py` — eliminar `O4_L1V1_DIR`

**Files:**
- Modify: `src/tfg_aves/ml/_paths.py`

- [ ] **Step 1: Borrar la línea de `O4_L1V1_DIR`**

Eliminar la línea `O4_L1V1_DIR: Path = ROOT / "data" / "processed" / "o4" / "l1_v1"`.

- [ ] **Step 2: Verificar que nadie más la importa**

Run: `grep -rn "O4_L1V1_DIR" src tests notebooks`
Expected: sin resultados (build.py ya no la usa tras Task 2).

- [ ] **Step 3: Commit**

```bash
git add src/tfg_aves/ml/_paths.py
git commit -m "O4/L1: elimina O4_L1V1_DIR (línea de viento retirada)"
```

---

## Task 4: Eliminar el paquete `meteo`, sus tests y las dependencias

**Files:**
- Delete: `src/tfg_aves/meteo/`, `tests/test_meteo_smoke.py`, `tests/test_meteo_wind.py`, `tests/test_meteo_build_wind.py`
- Modify: `pyproject.toml`, posiblemente `tests/test_reporting.py`

- [ ] **Step 1: Comprobar la referencia de `test_reporting.py` a meteo**

Run: `grep -n "meteo\|wind\|l1_v1\|L1V1" tests/test_reporting.py`
Si aparece algún import o uso de `meteo`/`O4_L1V1_DIR`/viento, eliminar esa parte del test (no debe quedar dependencia del paquete meteo).

- [ ] **Step 2: Borrar el paquete y los tests de meteo**

```bash
git rm -r src/tfg_aves/meteo
git rm tests/test_meteo_smoke.py tests/test_meteo_wind.py tests/test_meteo_build_wind.py
```

- [ ] **Step 3: Quitar las dependencias de viento de `pyproject.toml`**

Eliminar las líneas `"netcdf4>=1.6",` y `"xarray>=2024.0",` de la lista de dependencias.

- [ ] **Step 4: Resincronizar el entorno**

Run: `uv sync`
Expected: termina sin error; `uv.lock` actualizado (sin netcdf4/xarray salvo que sean transitivas).

- [ ] **Step 5: Verificar que no queda rastro de meteo/viento en el código fuente**

Run: `grep -rn "meteo\|with_wind\|wind_df\|merge_wind" src tests`
Expected: sin resultados.

- [ ] **Step 6: Ejecutar la suite completa**

Run: `uv run pytest -q`
Expected: PASS (sin los tests de meteo; el resto verde).

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "O4/L1: elimina el paquete meteo, sus tests y las deps de viento"
```

---

## Task 5: Borrar artefactos y datos del viento

**Files:**
- Delete: `data/processed/o4/l1_v1/`, `data/processed/wind_per_fix.parquet`, `data/raw/wind/`

- [ ] **Step 1: Borrar (son gitignored/regenerables; usar `rm`, no `git rm`)**

```bash
rm -rf data/processed/o4/l1_v1 data/processed/wind_per_fix.parquet data/raw/wind
```

- [ ] **Step 2: Verificar**

Run: `ls data/processed/o4/`
Expected: ya no aparece `l1_v1`.

(No hay commit: son ficheros no versionados.)

---

## Task 6: Regenerar los artefactos `o4/` con el nuevo `build_o4` (datos reales)

**Files:**
- Genera: `data/processed/o4/{model_*.pkl, predictions_test.parquet, metrics.parquet}`

- [ ] **Step 1: Ejecutar `build_o4()` sobre los datos reales**

Run: `uv run python -c "from tfg_aves.ml import build_o4; r = build_o4(seed=0); print(r.individual_bird_id, r.n_birds, r.n_rows_test)"`
Expected: imprime `91916A 82 <n>`; tarda varios minutos (entrena RF/XGB/LGBM poblacional + RF/XGB individual).

- [ ] **Step 2: Inspeccionar las métricas regeneradas**

Run:
```bash
uv run python -c "
import pandas as pd
m = pd.read_parquet('data/processed/o4/metrics.parquet')
print(sorted(m['modo'].unique()))
print(sorted(m['modelo'].unique()))
print(m[m['split']=='test'][['modelo','modo','top1','log_loss','dist_median_km']].to_string())
"
```
Expected: `modo` incluye `individual`, `poblacional`, `poblacional@91916A`, `—`; `modelo` incluye `persistencia`, `persistencia@91916A`, `markov`, `markov@91916A`, `rf`, `xgb`, `lgbm`.

- [ ] **Step 3: Sanity-check biológico (regla durable del TFG)**

Run:
```bash
uv run python -c "
import pandas as pd
p = pd.read_parquet('data/processed/o4/predictions_test.parquet')
ind = p[p['modo']=='individual']
print('aves en individual:', ind['bird_id'].unique())
print('dist mediana individual (km):', ind['pred_dist_km'].median())
"
```
Expected: única ave `['91916A']`; distancia mediana razonable (orden de decenas de km), sin saltos imposibles.

(No hay commit: los artefactos son gitignored.)

---

## Task 7: Reescribir las figuras del notebook `04_eda_o4.py` y regenerarlas

**Files:**
- Modify: `notebooks/04_eda_o4.py`
- Delete: `reports/figures/o4_fig06_error-by-state-personalizado.{png,csv,md}`, `reports/captions/o4_fig06_error-by-state-personalizado.md`, `reports/tables/o4_tab06_error-by-state-personalizado.csv`, y los equivalentes de `o4_fig08_personalizado-vs-poblacional` (figs/tables/captions).

> Contexto: `o4_fig01` (hyperparameter-config) NO cambia. Cambian C1→fig02, C2→fig03, C3→fig04, C4→fig05, C5/C6→fig06/07, C7→fig08, C8→fig09. Tras la celda A.2 el notebook tiene `result`, `metrics`, `preds`/`preds_all`. Añadir, justo después de cargar `metrics` (tras la línea `metrics = metrics.sort_values(...)`), una constante de ave:
>
> ```python
> BIRD = result.individual_bird_id
> POB_AT = f"poblacional@{BIRD}"
> ```

- [ ] **Step 1: C1 → fig02 (gap individual vs poblacional@ave)**

Reemplazar el bloque de la celda C1 (desde `gap = (` hasta el `save_artifact(... num=2 ...)` incluido) por:

```python
gap = (
    metrics
    .pivot_table(index=["modelo", "modo"], columns="split",
                 values=["top1", "log_loss", "dist_median_km"])
    .reset_index()
)
gap.columns = ["_".join([str(c) for c in col if c]).strip("_") for col in gap.columns]
gap["gap_top1"] = gap["top1_train"] - gap["top1_test"]
gap["gap_log_loss"] = gap["log_loss_test"] - gap["log_loss_train"]
# Comparación sobre la misma ave: individual vs poblacional@ave (RF/XGB).
gap_view = gap[gap["modo"].isin(["individual", POB_AT])].copy()
gap_view = gap_view[gap_view["modelo"].isin(["rf", "xgb"])]
gap_view.sort_values(["modo", "modelo"])
```

```python
fig_c1, axes = plt.subplots(1, 2, figsize=(13, 4))
fams_order = ["rf", "xgb"]
modes_order = ["individual", POB_AT]
x = np.arange(len(fams_order))
width = 0.35
for ax_idx, (col_train, col_test, title) in enumerate([
    ("top1_train", "top1_test", "Top-1 (mayor mejor)"),
    ("log_loss_train", "log_loss_test", "Log-loss (menor mejor)"),
]):
    ax = axes[ax_idx]
    for i, mode in enumerate(modes_order):
        sub = gap_view[gap_view["modo"] == mode].set_index("modelo")
        train_v = sub.loc[fams_order, col_train].to_numpy()
        test_v = sub.loc[fams_order, col_test].to_numpy()
        offset = (i - 0.5) * width
        ax.bar(x + offset - width / 4, train_v, width / 2, label=f"{mode} train", alpha=0.6)
        ax.bar(x + offset + width / 4, test_v, width / 2, label=f"{mode} test")
    ax.set_xticks(x); ax.set_xticklabels(fams_order)
    ax.set_title(title); ax.legend(fontsize=8)
fig_c1.suptitle(f"C1 — Overfit (train vs test) sobre {BIRD}: individual vs poblacional@{BIRD}")
fig_c1.tight_layout()

save_artifact(
    slug="train-test-gap",
    objective="o4",
    num=2,
    decision="Diagnóstico del gap train-test (overfit) del individual frente al poblacional sobre 91916A",
    caption_es=(
        "Top-1 (izquierda) y log-loss (derecha) evaluados sobre train y test, "
        f"todo restringido a las filas del ave {BIRD}: el modelo individual "
        f"(entrenado solo con {BIRD}) frente al poblacional evaluado sobre esas "
        "mismas filas (poblacional@" + BIRD + "). La diferencia train→test es el "
        "indicador empírico del overfit. La comparación es manzanas-con-manzanas "
        "porque ambos se miden sobre el mismo conjunto de evaluación; el modo "
        "poblacional global se analiza aparte en C3."
    ),
    fig=fig_c1,
    table=gap_view,
    overwrite=True,
)
```

- [ ] **Step 2: C2 → fig03 (learning-curves: poblacional xgb/lgbm + individual xgb)**

Reemplazar el bloque de carga de `curves` y la figura por:

```python
import joblib

curves = {}
for mode, family in [("poblacional", "xgb"), ("poblacional", "lgbm"), ("individual", "xgb")]:
    bundle = joblib.load(result.model_paths[f"{mode}_{family}"])
    model = bundle["model"]
    if family == "xgb":
        inner = model._xgb
        losses = inner.evals_result()["validation_0"]["mlogloss"]
        best_iter = getattr(inner, "best_iteration", None)
    else:
        inner = model._lgbm
        losses = inner.evals_result_["valid_0"]["multi_logloss"]
        best_iter = getattr(inner, "best_iteration_", None)
    curves[(mode, family)] = (losses, best_iter)
```

```python
fig_c2, axes = plt.subplots(1, 2, figsize=(13, 4))
# Panel XGB: poblacional vs individual. Panel LGBM: solo poblacional (sin individual).
panel_curves = {
    "xgb": [("poblacional", "xgb"), ("individual", "xgb")],
    "lgbm": [("poblacional", "lgbm")],
}
for ax, family in zip(axes, ["xgb", "lgbm"]):
    for mode, fam in panel_curves[family]:
        losses, best_iter = curves[(mode, fam)]
        ax.plot(losses, label=mode)
        if best_iter is not None:
            ax.axvline(best_iter, linestyle="--", alpha=0.4)
    ax.set_xlabel("Iteración"); ax.set_ylabel("log-loss (val)")
    ax.set_title(family.upper()); ax.legend()
fig_c2.suptitle("C2 — Curvas de aprendizaje (val) — la línea punteada marca early stopping")
fig_c2.tight_layout()
```

En el `save_artifact(... num=3 ...)` correspondiente, sustituir en `caption_es` la frase "en ambos modos" por "para el poblacional (XGB y LightGBM) y el individual de 91916A (solo XGB)"; el resto del caption se conserva.

- [ ] **Step 3: C3 → fig04 (comparativa GLOBAL: poblacional + baselines)**

En la celda C3, restringir `test_metrics` a la comparación global (poblacional + baselines), excluyendo individual y los cortes @ave:

```python
test_metrics = metrics[
    (metrics["split"] == "test")
    & (metrics["modo"].isin(["poblacional", "—"]))
].copy()
```

(El resto de la celda C3 —`combo`, `move_top1`, `c3_table`, la figura de 5 paneles— no cambia.) En el `save_artifact(... num=4 ...)`, cambiar el `decision` a `"Comparativa global sobre las 82 aves: poblacional (3 familias) + baselines"` y en `caption_es` sustituir "los 8 modelos / 3 familias × 2 modos" por "el modo poblacional (RF/XGB/LightGBM) frente a las baselines"; quitar la mención a "personalizado".

- [ ] **Step 4: C4 → fig05 (ganador poblacional global + ganador individual sobre 91916A)**

Reemplazar el bloque de cómputo de ganadores por:

```python
ml_only = c3_table[c3_table["modelo"].isin(["rf", "xgb", "lgbm"])].copy()
ganador_pob = ml_only.sort_values("log_loss").iloc[0]

# Ganador individual: sobre las métricas del modo individual (RF/XGB), test.
ind_metrics = metrics[
    (metrics["split"] == "test") & (metrics["modo"] == "individual")
].copy()
ganador_ind = ind_metrics.sort_values("log_loss").iloc[0]
print(f"Ganador poblacional (global): {ganador_pob['modelo']} (log-loss={ganador_pob['log_loss']:.3f})")
print(f"Ganador individual ({BIRD}): {ganador_ind['modelo']} (log-loss={ganador_ind['log_loss']:.3f})")
```

Reemplazar `recomendacion_es` y `c4_table` por:

```python
recomendacion_es = (
    "**Criterio principal: log-loss** (calibración probabilística, heredado de O2). "
    f"**Ganador poblacional (82 aves):** `{ganador_pob['modelo']}` con "
    f"log-loss = {ganador_pob['log_loss']:.3f}. "
    f"**Ganador individual ({BIRD}):** `{ganador_ind['modelo']}` con "
    f"log-loss = {ganador_ind['log_loss']:.3f} (evaluado solo sobre {BIRD}). "
    "\n\n"
    "El poblacional es el modelo canónico (generaliza a las 82 aves). El "
    f"individual se entrena exclusivamente con {BIRD} y solo es comparable contra "
    f"el poblacional restringido a esa ave (poblacional@{BIRD}); esa comparación "
    "se analiza en C7. LightGBM queda descartado por la divergencia de C2."
)

final_rows = []
for tag, row in [("poblacional (global)", ganador_pob), (f"individual ({BIRD})", ganador_ind)]:
    final_rows.append({
        "rol": f"ganador {tag}", "modelo": row["modelo"], "modo": row["modo"],
        "top1": row["top1"], "log_loss": row["log_loss"], "dist_median_km": row["dist_median_km"],
    })
for name in ["persistencia", "markov"]:
    row = c3_table[c3_table["modelo"] == name].iloc[0]
    final_rows.append({
        "rol": f"baseline {name}", "modelo": name, "modo": "—",
        "top1": row["top1"], "log_loss": row["log_loss"], "dist_median_km": row["dist_median_km"],
    })
c4_table = pd.DataFrame(final_rows)
```

En el `save_artifact(... num=5 ...)`, cambiar `decision` a:
```python
    decision=(
        f"Ganador poblacional = {ganador_pob['modelo']}; "
        f"ganador individual {BIRD} = {ganador_ind['modelo']}. Criterio: log-loss."
    ),
```

- [ ] **Step 5: C5/C6 → fig06 (error-by-state-individual) + fig07 (poblacional)**

Sustituir el cómputo de tablas por estado:

```python
from tfg_aves.ml.evaluate import evaluate_by_state


def _table_for(family: str, mode: str) -> pd.DataFrame:
    sub = preds_all[(preds_all["modelo"] == family) & (preds_all["modo"] == mode)]
    table = evaluate_by_state(sub, state_col="state_b_causal")
    table["modelo"] = family; table["modo"] = mode
    return table


c5_table = _table_for(ganador_ind["modelo"], "individual")
c6_table = _table_for(ganador_pob["modelo"], "poblacional")
display(c5_table); display(c6_table)
```

En la figura `fig_state`, cambiar la lista de etiquetas de `[(c5_table, "personalizado"), (c6_table, "poblacional")]` a `[(c5_table, f"individual {BIRD}"), (c6_table, "poblacional")]`.

Cambiar el primer `save_artifact` (num=6) a slug **`error-by-state-individual`**, con:
```python
    decision="Top-1/top-3 por estado HMM causal para el modelo individual de 91916A",
    caption_es=(
        f"Accuracy del modelo individual de {BIRD} ({ganador_ind['modelo']}, "
        "entrenado solo con esa ave) desglosada por estado biológico (state_b_causal). "
        "Se mantiene el patrón del TFG: acierto alto en días estacionarios y caída "
        "en migración, ahora medido sobre un único individuo con ruta propia."
    ),
```
El segundo `save_artifact` (num=7, slug `error-by-state-poblacional`) conserva slug; en su `caption_es` quitar la frase comparativa con "personalizado" (sustituir el párrafo "El patrón es prácticamente idéntico al personalizado…" por "El patrón es el característico del pipeline: estacionario alto y migración baja, sobre las 82 aves.").

- [ ] **Step 6: C7 → fig08 (individual-vs-poblacional@ave)**

Reemplazar TODO el bloque C7 (desde `c7_rows = []` hasta el `save_artifact(... num=8 ...)` incluido) por:

```python
# C7 — Individual (91916A) vs poblacional@91916A + baselines, sobre el test de la ave.
contenders = [
    ("individual", "rf"), ("individual", "xgb"),
    (POB_AT, "rf"), (POB_AT, "xgb"),
]
test_m = metrics[metrics["split"] == "test"]
c7_rows = []
for modo, fam in contenders:
    r = test_m[(test_m["modo"] == modo) & (test_m["modelo"] == fam)]
    if len(r) == 0:
        continue
    r = r.iloc[0]
    c7_rows.append({"etiqueta": f"{fam}/{modo}", "top1": r["top1"], "top3": r["top3"],
                    "log_loss": r["log_loss"], "dist_median_km": r["dist_median_km"]})
for name in [f"persistencia@{BIRD}", f"markov@{BIRD}"]:
    r = test_m[test_m["modelo"] == name]
    if len(r) == 0:
        continue
    r = r.iloc[0]
    c7_rows.append({"etiqueta": name, "top1": r["top1"], "top3": r["top3"],
                    "log_loss": r["log_loss"], "dist_median_km": r["dist_median_km"]})
c7_table = pd.DataFrame(c7_rows)
print(c7_table.to_string())

fig_c7, axes_c7 = plt.subplots(1, 3, figsize=(18, 4.5))
for ax, (metric, title, asc) in zip(axes_c7, [
    ("top1", "Top-1 (mayor mejor)", False),
    ("log_loss", "Log-loss (menor mejor)", True),
    ("dist_median_km", "Dist mediana km (menor mejor)", True),
]):
    sub = c7_table.dropna(subset=[metric]).sort_values(metric, ascending=asc)
    ax.barh(range(len(sub)), sub[metric].to_numpy())
    ax.set_yticks(range(len(sub))); ax.set_yticklabels(sub["etiqueta"], fontsize=8)
    ax.invert_yaxis(); ax.set_title(title, fontsize=9)
fig_c7.suptitle(f"C7 — Individual vs poblacional@{BIRD} (test de {BIRD})")
fig_c7.tight_layout()

save_artifact(
    slug="individual-vs-poblacional",
    objective="o4",
    num=8,
    decision=(
        f"Comparar el modelo per-individuo de {BIRD} contra el poblacional "
        f"evaluado sobre {BIRD} (poblacional@{BIRD}) y las baselines, mismas filas"
    ),
    caption_es=(
        f"Comparación manzanas-con-manzanas sobre el test del ave {BIRD}: el "
        f"modelo individual (RF/XGB entrenados solo con {BIRD}) frente al modelo "
        f"poblacional restringido a {BIRD} (poblacional@{BIRD}, RF/XGB) y las "
        f"baselines persistencia y Markov sobre esas mismas filas. Replica la "
        f"metodología de L3: si el individual no supera claramente a "
        f"poblacional@{BIRD}, se confirma que entrenar por-individuo no aporta "
        "frente al modelo global, reforzando la decisión 'global vs per-individuo' "
        "a favor del global."
    ),
    fig=fig_c7,
    table=c7_table,
    overwrite=True,
)
```

- [ ] **Step 7: C8 → fig09 (feature-importance: ganador poblacional + ganador individual)**

En la función `_feature_importance`, las claves de `result.model_paths` ya son `f"{mode}_{family}"`. Reemplazar las dos llamadas y la figura por:

```python
imp_pob = _feature_importance("poblacional", ganador_pob["modelo"])
imp_ind = _feature_importance("individual", ganador_ind["modelo"])

fig_c8, axes = plt.subplots(1, 2, figsize=(13, 4.5))
for ax, df, title in zip(
    axes, [imp_pob, imp_ind],
    [f"Ganador poblacional ({ganador_pob['modelo']})",
     f"Individual {BIRD} ({ganador_ind['modelo']})"],
):
    ax.barh(df["feature"], df["importance"]); ax.invert_yaxis(); ax.set_title(title)
fig_c8.suptitle("C8 — Importancia de features: poblacional vs individual")
fig_c8.tight_layout()
c8_table = pd.concat([imp_pob, imp_ind], ignore_index=True)
```

En el `save_artifact(... num=9 ...)`: cambiar `decision` a `"Verificar que state_b_causal y posterior_b_migracion_causal se usan en ambos modelos"` y reescribir `caption_es` para que no mencione `bird_id` (ya no es feature): describir que lat/lon dominan y que las features de O3 aportan de forma secundaria, en el poblacional y en el individual de 91916A.

- [ ] **Step 8: Borrar los artefactos antiguos con slug de "personalizado"**

```bash
git rm reports/figures/o4_fig06_error-by-state-personalizado.png \
       reports/captions/o4_fig06_error-by-state-personalizado.md \
       reports/tables/o4_tab06_error-by-state-personalizado.csv \
       reports/figures/o4_fig08_personalizado-vs-poblacional.png \
       reports/captions/o4_fig08_personalizado-vs-poblacional.md \
       reports/tables/o4_tab08_personalizado-vs-poblacional.csv
```
(Ajustar según los ficheros realmente presentes: `ls reports/figures/o4_fig0[68]_* reports/tables/o4_tab0[68]_* reports/captions/o4_fig0[68]_*`.)

- [ ] **Step 9: Ejecutar el notebook para regenerar las figuras**

Run: `uv run jupytext --to notebook --execute --output /tmp/o4_run.ipynb notebooks/04_eda_o4.py`
Expected: ejecuta sin error; se regeneran `reports/figures/o4_fig02..09.png` y se crean `o4_fig06_error-by-state-individual.*` y `o4_fig08_individual-vs-poblacional.*`.

- [ ] **Step 10: Verificar los nuevos ficheros**

Run: `ls reports/figures/o4_fig0[2-9]_*.png && grep -L "personalizado" reports/captions/o4_fig0[2-9]_*.md`
Expected: existen las 8 figuras; ninguna caption nueva menciona "personalizado".

- [ ] **Step 11: Commit**

```bash
git add notebooks/04_eda_o4.py reports/figures/o4_fig0*.png reports/figures/o4_fig0*.pdf \
        reports/tables/o4_tab0*.csv reports/captions/o4_fig0*.md
git add -A reports/figures reports/tables reports/captions
git commit -m "O4/L1: regenera figuras 02-09 con el modelo individual de 91916A"
```

---

## Task 8: Borrar figuras, notebook y spec/plan del viento; actualizar INDEX.md

**Files:**
- Delete: `reports/figures/o4_fig1[0-6]_*`, `reports/tables/o4_tab1[0-6]_*`, `reports/captions/o4_fig1[0-6]_*`, `notebooks/04l1_eda_o4l1.{py,ipynb}`, `docs/superpowers/specs/2026-05-23-o4l1-features-design.md`, `docs/superpowers/plans/2026-05-23-o4l1-features-viento.md`
- Modify: `reports/INDEX.md`

- [ ] **Step 1: Borrar figuras/tablas/captions del viento (o4_fig10–16)**

```bash
git rm reports/figures/o4_fig1[0-6]_*.png reports/tables/o4_tab1[0-6]_*.csv reports/captions/o4_fig1[0-6]_*.md
```
(Algunas pueden no tener tabla; usar `ls` antes y borrar solo los existentes.)

- [ ] **Step 2: Borrar el notebook y el spec/plan del viento**

```bash
git rm notebooks/04l1_eda_o4l1.py notebooks/04l1_eda_o4l1.ipynb \
       docs/superpowers/specs/2026-05-23-o4l1-features-design.md \
       docs/superpowers/plans/2026-05-23-o4l1-features-viento.md
```

- [ ] **Step 3: Limpiar `reports/INDEX.md`**

- Eliminar las filas de la tabla cuyo ref empiece por `o4_fig10`…`o4_fig16` (las 7 del viento, slugs `l1v1-*`).
- Renombrar la fila `o4_fig06_error-by-state-personalizado` → `o4_fig06_error-by-state-individual` (slug, ref, rutas `.png/.csv/.md` y descripción de decisión).
- Renombrar la fila `o4_fig08_personalizado-vs-poblacional` → `o4_fig08_individual-vs-poblacional` (idem) y actualizar la descripción.

Run para localizar: `grep -n "o4_fig1[0-6]\|personalizado" reports/INDEX.md`

- [ ] **Step 4: Verificar coherencia de INDEX.md con el disco**

Run:
```bash
uv run python -c "
import re, pathlib
idx = pathlib.Path('reports/INDEX.md').read_text()
refs = re.findall(r'reports/figures/(o4_fig\d+_[\w-]+)\.png', idx)
missing = [r for r in refs if not pathlib.Path(f'reports/figures/{r}.png').exists()]
print('refs O4 en INDEX que faltan en disco:', missing)
"
```
Expected: lista vacía.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "O4/L1: retira figuras, notebook y spec/plan de la línea del viento; actualiza INDEX"
```

---

## Task 9: Actualizar notas de memoria y registrar en ai-log

**Files:**
- Modify: `reports/memoria/06_o4_ml.md`
- Create: `reports/ai-log/NNNN-o4l1-individual-91916a.md`

- [ ] **Step 1: Reescribir la sección de L1 en `06_o4_ml.md`**

Run: `grep -n "personalizado\|bird_id\|memoriz\|viento\|wind\|L1" reports/memoria/06_o4_ml.md`
- Sustituir la narrativa "bird_id no memoriza / el personalizado aporta ~1 pp" por la nueva: en L1 el segundo brazo es un modelo **individual** de 91916A (no `bird_id` como feature), comparado contra `poblacional@91916A`; el hallazgo se alinea con L3 (per-individuo no supera al global) y refuerza el eje "global vs per-individuo".
- Eliminar cualquier subsección dedicada a la línea del viento (L1-v1) y sus referencias a `o4_fig10–16`.
- Actualizar las referencias de figura: `o4_fig06_error-by-state-personalizado` → `…-individual`; `o4_fig08_personalizado-vs-poblacional` → `…individual-vs-poblacional`.

- [ ] **Step 2: Crear la entrada de ai-log**

Run: `ls reports/ai-log/ | tail -3` para el siguiente número `NNNN`. Crear `reports/ai-log/NNNN-o4l1-individual-91916a.md` siguiendo la estructura de `reports/ai-log/README.md`: tarea = "L1: sustituir el modo personalizado (bird_id como feature) por un modelo individual real de 91916A (patrón L3) y retirar la línea del viento; regenerar figuras". Tono que ensalza la autoría (la crítica al etiquetado 'personalizado' y la decisión de retirar el viento son del autor; la IA ejecutó el rework y replicó el patrón L3).

- [ ] **Step 3: Commit**

```bash
git add reports/memoria/06_o4_ml.md reports/ai-log/
git commit -m "O4/L1: actualiza notas de memoria (individual 91916A) y registra en ai-log"
```

---

## Task 10: Verificación final

- [ ] **Step 1: Suite completa + ruff**

Run: `uv run pytest -q && uv run ruff check src tests`
Expected: todo verde, ruff limpio.

- [ ] **Step 2: Sin rastro del viento ni de "personalizado" en L1**

Run:
```bash
grep -rn "meteo\|with_wind\|wind_df\|merge_wind\|O4_L1V1" src tests
grep -rn "o4_fig1[0-6]\|l1v1\|l1_v1" reports/INDEX.md notebooks/04_eda_o4.py
grep -rn "personalizado" notebooks/04_eda_o4.py src/tfg_aves/ml/build.py
```
Expected: las tres búsquedas sin resultados (en `build_l2.py` SÍ sigue habiendo "personalizado" — es correcto, L2 no se toca).

- [ ] **Step 3: Resumen al autor**

Reportar: métricas test del individual (RF/XGB) vs poblacional@91916A vs baselines@91916A, y confirmar el sanity-check biológico. Señalar la inconsistencia deliberada (L2 conserva `personalizado`) por si el autor quiere alinearla después.

---

## Self-review (rellenado por el autor del plan)

- **Cobertura del spec:** F1–F10 cubiertos (F1/F2/F3/F4 Task 2; F5 Task 2; F6 Task 2 corte @ave + baselines@ave; F7 sin cambios; F8 log-loss en figuras; F9 Tasks 1-4 conservan include_bird_id, L2/L3 intactos; F10 Tasks 3-5,8). Figuras §7 → Task 7. Decisiones sin figura §8 → Task 9 (memoria). Criterios §9 → Task 10.
- **Placeholders:** ninguno; todo el código está explícito. Los `NNNN` del ai-log y los nombres exactos de ficheros a `git rm` se resuelven con el `ls`/`grep` indicado en el propio step.
- **Consistencia de tipos/nombres:** `model_paths` usa claves `f"{mode}_{family}"` en build.py y en el notebook (`result.model_paths[f"{mode}_{family}"]`); `modo` toma `individual`/`poblacional`/`poblacional@<ave>`; baselines `persistencia@<ave>`/`markov@<ave>`. `compare_models` parte por el primer `_`, compatible con `@<ave>`. `BuildO4Result.individual_bird_id` añadido y leído en el notebook (`BIRD = result.individual_bird_id`).
